package br.org.funcate.glue.model.cache;

/**
 * \brief Enum thats represents the status that a tile could have.
 * 
 * @author Juliana
 * @version 1.0.0
 */
public enum TileStatus {
	LOADING, LOADED, NOT_LOADED;
}
